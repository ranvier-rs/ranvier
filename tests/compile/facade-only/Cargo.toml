[package]
name = "ranvier-facade-compile-contract"
version = "0.0.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
ranvier = { path = "../../../kit" }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
