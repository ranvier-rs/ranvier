# Ranvier Examples

> **The Reference:** Proof-of-concept implementations and patterns.

## 🎯 Purpose
Demonstrates how to use the Ranvier ecosystem in real-world scenarios.

## 📂 Projects

### 1. `basic-schematic`
- **Goal:** Verify the Core traits and Netlist generation.
- **Features:** 
  - Uses `ranvier-core` + `ranvier-macros`.
  - Defines a pipeline with 3 async steps.
  - Prints the JSON Schema (introspection output).
  - Runs the pipeline execution.

### 2. `hello-world` (Planned)
- **Goal:** Demonstrate HTTP server integration.
- **Features:**
  - Uses `ranvier-http`.
  - Serves a basic plaintext response.

### 3. `routing-demo` (Planned)
- **Goal:** Show dynamic routing and context usage.
