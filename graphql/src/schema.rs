/// Helper to bind Axon transitions to GraphQL resolvers via context.
pub struct SchemaBuilder {
    // We can store a reference to Axon or mapping metadata here
}

impl SchemaBuilder {
    pub fn new() -> Self {
        Self {}
    }
}
